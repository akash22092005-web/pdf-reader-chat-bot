{
  "installCommand": "test -d frontend && cd frontend || true; npm install && chmod +x node_modules/.bin/* || true",
  "buildCommand": "test -d frontend && cd frontend || true; npx vite build",
  "outputDirectory": "dist"
}
