{
  "name": "pdf-reader-chat-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "chmod +x frontend/node_modules/.bin/* || true",
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build"
  }
}
